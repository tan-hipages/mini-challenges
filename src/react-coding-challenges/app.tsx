import React from 'react';
import ReactDOM from 'react-dom';
import UserList from './03-react-component';

ReactDOM.render(
  <React.StrictMode>
    <UserList />
  </React.StrictMode>,
  document.getElementById('root')
);
